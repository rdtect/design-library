# Changesets

This directory contains changesets for automated versioning and publishing.

Each PR that changes a package should include a `.changeset/<unique-name>.md` file.
Run `pnpm changeset` to generate one interactively.

See [changesets docs](https://github.com/changesets/changesets).
